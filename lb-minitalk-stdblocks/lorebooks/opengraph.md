identifier=opengraph
Use for a website shared in the conversation. Write `content` on a single line as `website name § URL § page title § description`. Omit the description when unnecessary. Write preview text consistent with the fictional setting and conversation. Treat the card as a generated preview, not fetched website metadata. Apply the enclosing TOON string escaping to the complete payload.
Allow a long description when the page needs one. Truncate the description at a reasonable length and append `(...)` when more text remains.
